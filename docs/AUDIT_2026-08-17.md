# Full Code Audit — isiHunt

**Stand:** 2026-08-17 · Version `0.1.124` · Commit `c0773b0`

Vollstaendiger Audit ueber `src/` (61 Dateien, ~14.400 Zeilen) und `docs/`
(2.308 Zeilen). Nur Auflistung, keine Codeaenderung an Produktivcode. Fuer
XP-/Coin-Balance siehe den separaten Bericht `docs/BALANCE_2026-08-17.md`.

Methodik: eigene Pruefung aller neun Code-Regeln plus die Skill
`codebase-improvement-audit` (Blindspot-Suche jenseits der gruenen
Testsuite). Jeder Fund unten ist mit Datei:Zeile belegt — was nicht geprueft
werden konnte, ist als offene Frage markiert, nicht als Tatsache verkauft.

---

## 1. Zusammenfassung

| Kategorie | Anzahl | Schwere |
|---|---|---|
| Doku/Code-Drift (Balancing-Werte) | 7 bestaetigte Diskrepanzen | Mittel — betrifft nur `GAME_DESIGN.md`, nicht Spielverhalten |
| Doku/Code-Drift (Architektur) | 15 fehlende Dateien in `ARCHITECTURE.md` | Niedrig — Doku veraltet, Code korrekt |
| Regelverstoss (Regel 1, magische Zahlen) | 8 Fundstellen | Niedrig-Mittel |
| **Faktischer Anzeigefehler** | 1 (`GameScene.ts:312`) | **Hoch — Behoben 2026-08-17** |
| Regel 4 (Listener-Abmeldung), offene Einordnung | 2 Dateien (`SafeAreaSystem`, `SoundSystem`) | Mittel — kein Leck im aktuellen Code, aber Wortlaut-Konflikt |
| Ungetestete `systems/`-Logik | 5 von 7 Dateien mit echter Verzweigungslogik | Mittel-Hoch |
| **Datenverlust-Pfad ohne Test** | 1 (`SaveSystem.ts`) | **Hoch — Behoben 2026-08-17, mit Regressionstest** |
| Blindspot-Funde (Skill-Methodik) | 13 konkrete Kandidaten | siehe Abschnitt 5 |
| Performance | 1 gemessen (kein Hotspot), 1 nicht messbar | Niedrig |
| Duplikat | 1 kleines (12 Zeilen, 2 Dateien) | Niedrig |

Die zwei Funde mit "Hoch" waren die einzigen, die ueber reine
Doku-Inkonsistenz hinausgingen und tatsaechliches Fehlverhalten im
Produktivcode belegten — beide sind seit 2026-08-17 behoben (siehe
Abschnitte 3.1 und 5.1). Alle anderen Funde in dieser Tabelle sind weiterhin
offen.

---

## 2. Baseline

`npm run verify` (Typecheck, Lint, Format, Tests, Build) lief vollstaendig
grn durch:

```
Test Files  5 passed (5)
     Tests  71 passed (71)
Build       ✓ built in 9.00s
```

**Blindspot-Karte — was jedes Gate beweist und was nicht:**

| Gate | Beweist | Sieht strukturell nicht |
|---|---|---|
| `tsc --noEmit` | Typkonsistenz | Laufzeitlogik, falsche aber typkorrekte Werte (z.B. der `-1,4 s`-String unten) |
| `eslint` | Stilregeln, einige Bugklassen (unused vars etc.) | Fachliche Korrektheit, Regel 1/2/4 aus CLAUDE.md (nicht automatisiert erzwungen) |
| `prettier --check` | Formatierung | Nichts Fachliches |
| `vitest run` (71 Tests, 5 Dateien) | Verhalten der 5 getesteten Systeme fuer die geschriebenen Faelle | Alles in `SaveSystem`, `CloudSystem`, `AuthSystem`, `SoundSystem`, `ProgressSyncSystem`, `SyncStatusSystem`, `SafeAreaSystem` (keine Tests) sowie ungetestete Zustandskombinationen in den getesteten Systemen (siehe Abschnitt 5) |
| `vite build` | Bundle baut, keine Modulfehler | Inhaltliche Korrektheit |

Das ist der Suchplan fuer die Abschnitte 4-6.

---

## 3. Regelkonformitaet (9 Regeln, CLAUDE.md / CODE_STYLE.md)

### Regel 1 — Keine magischen Zahlen ausserhalb `src/config/`

**Status: Verstoesse gefunden.** Vollstaendiger Scan von `scenes/`,
`systems/`, `entities/`, `ui/`, `input/`. Balancing-relevante Funde:

- [SpawnSystem.ts:96](src/systems/SpawnSystem.ts#L96) — `realInRange(0.8, 1.2)`, Streuungsfaktor fuers Spawn-Intervall
- [SpawnSystem.ts:112](src/systems/SpawnSystem.ts#L112) — `Math.min(0.24, ...)`, Obergrenze Hindernis-Wahrscheinlichkeit
- [SpawnSystem.ts:129](src/systems/SpawnSystem.ts#L129) — `Math.max(0.55, 1 - (...) * 0.35)`, Formelkonstanten fuer `worldLifetimeScale`
- [ChallengeSystem.ts:65](src/systems/ChallengeSystem.ts#L65) — Bot-Schwierigkeitsfaktoren `{easy: 0.72, normal: 0.9, hard: 1.04}`
- [ChallengeSystem.ts:66](src/systems/ChallengeSystem.ts#L66) — Streuungsformel `((hashSeed(...) % 13) - 6) / 100`
- [ProgressionSystem.ts:40-42](src/systems/ProgressionSystem.ts#L40-L42) — Coin-Bonusformel mit Divisoren `5`, `2`, Multiplikator `3`
- [InputController.ts:81](src/input/InputController.ts#L81) — `distance / 90`, Mindestfaktor `0.15`

Kosmetische Werte (Kamera-Shake, Partikel, Tween-Dauern in `GameScene.ts`,
`Obstacle.ts`, `Collectible.ts`, `widgets.ts`, `SoundSystem.ts`) wurden
gesichtet, aber nicht einzeln aufgelistet — sie haben keinen
Balancing-Charakter im Sinne der Regel.

**Zusatzfund, schwerer als ein reiner Regel-1-Verstoss — Behoben
2026-08-17:** [GameScene.ts:312](src/scenes/GameScene.ts#L312) zeigte bei
einem Penalty-Treffer den Text `'-1,4 s'` an. Der tatsaechliche Zeitverlust
war [WORLD_PENALTY_MS = 800](src/config/GameConfig.ts#L139), also **0,8
Sekunden** — eine falsche Information an den Spieler, vermutlich ein Rest
einer frueheren Balancing-Version, bei der `WORLD_PENALTY_MS` noch 1400
war. Fix: Der Text wird jetzt aus `WORLD_PENALTY_MS` berechnet statt
hartcodiert zu sein, damit derselbe Drift bei kuenftigen Balancing-Aenderungen
nicht wieder auftreten kann.

### Regel 2 — Kommentare erklaeren warum, nie was

Nicht systematisch vollstaendig pruefbar (nur durch Lesen, kein
Werkzeugbeleg moeglich). Stichprobe in Dateien >200 Zeilen ergab keine
auffaelligen Was-Kommentare; mehrere Kommentare mit Warum-Charakter
gefunden (z.B. [GameConfig.ts:150-153](src/config/GameConfig.ts#L150-L153)
zu `COMBO_GRACE_MS`). Kein systematischer Verstoss identifiziert, aber diese
Aussage ist schwaecher belegt als die uebrigen — echte Vollstaendigkeit
haette eine Lektuere aller 61 Dateien erfordert, die hier nicht geleistet
wurde.

### Regel 3 — Importe zeigen nur nach unten

**Status: vollstaendig konform.** Vollstaendiger Import-Graph aus `src/`
aufgebaut. `config/` importiert nichts aus `systems/scenes/entities/ui`;
`systems/` importiert nichts aus `scenes/entities/ui`; `entities/`
importiert nichts aus `scenes/systems`; `ui/` importiert nichts aus
`scenes/entities` (nur `ui/widgets.ts → systems/SoundSystem`, das ist
regelkonform). Keine Verstoesse.

### Regel 4 — Jeder `eventBus.onEvent` braucht ein `offEvent`

**Status: 2 offene Faelle, kein akutes Leck.** Alle `onEvent`-Aufrufe im
Projekt:

| Datei | Zeilen | `offEvent` vorhanden |
|---|---|---|
| [GameScene.ts:174-175](src/scenes/GameScene.ts#L174-L175) | 2 | Ja, in `cleanup()` via SHUTDOWN-Handler |
| [HudScene.ts:373-377](src/scenes/HudScene.ts#L373-L377) | 5 | Ja, in `unregisterEvents()` via SHUTDOWN-Handler |
| [SafeAreaSystem.ts:84-86](src/systems/SafeAreaSystem.ts#L84-L86) | 3 | **Nein** |
| [SoundSystem.ts:292-295](src/systems/SoundSystem.ts#L292-L295) | 4 | **Nein** |

Beide betroffenen Dateien sind **keine Phaser-Scenes**, sondern Module mit
einer `initialize()`-Funktion, exakt einmal aus
[main.ts:109-110](src/main.ts#L109-L110) aufgerufen. Der in CODE_STYLE.md
1.4 beschriebene SHUTDOWN-Mechanismus setzt eine Scene voraus und ist
strukturell nicht anwendbar. Kein alternativer Unsubscribe-Mechanismus
existiert; die Listener leben fuer die gesamte App-Laufzeit. Das ist im
aktuellen Code **kein Fehler** (kein Restart-Pfad ruft `initialize()` ein
zweites Mal auf — `SoundSystem` hat sogar einen expliziten Re-Init-Guard,
[SoundSystem.ts:300-301](src/systems/SoundSystem.ts#L300-L301);
`SafeAreaSystem.registerGameEvents()` hat **keinen** solchen Guard).

**Offene Frage, keine Bewertung:** Der Wortlaut in CODE_STYLE.md ("Jeder
Listener wird abgemeldet [...] **Ausnahmslos**") nennt keine Ausnahme fuer
Nicht-Scene-Module. Das Dokument entscheidet nicht, ob App-Lebenszyklus-Module
regelkonform sind, weil sie den `eventBus` selbst ueberdauern sollen, oder ob
sie einen zweiten Ausnahmefall neben `SpawnSystem` (Regel 6) brauchen. Sollte
`SafeAreaSystem.registerGameEvents()` je aus einem anderen Kontext als
`initialize()` aufgerufen werden, wuerden die drei Listener doppelt
registrieren — aktuell nicht der Fall, aber ungeschuetzt.

### Regel 5 — Alles Bewegte rechnet mit `delta`

**Status: vollstaendig konform.** `GameScene.update(_time, delta)` berechnet
`dtSec` und reicht es konsequent durch. `Player.move()`, `Collectible.tick()`,
`Obstacle.tick()` skalieren alle Bewegungs-/Alterungs-/Cooldown-Werte mit
`dtSec`/`deltaMs`. Keine zeitunabhaengigen Additionen gefunden.

### Regel 6 — `systems/` kennt Phaser nicht

**Status: vollstaendig konform.** Einziger `from 'phaser'`-Import in
`systems/` ist die dokumentierte Ausnahme
[SpawnSystem.ts:30](src/systems/SpawnSystem.ts#L30).

### Regel 7 — Nur Daten ueber den EventBus

**Status: vollstaendig konform.** Alle Payload-Typen in
[EventBus.ts](src/core/EventBus.ts) sind reine Daten. Alle 13
`emitEvent`-Aufrufe im Projekt geprueft — keine Funktionen, GameObjects oder
Scene-Referenzen als Payload.

### Regel 8 — No-Guess-Vertrag

Meta-Regel, durch die Belegpflicht dieses Berichts selbst eingehalten. Jede
Aussage oben traegt einen Datei:Zeile-Verweis oder ist explizit als
Stichprobe/offene Frage markiert.

### Regel 9 — Versionierung/Deploy-Kette

**Status: vollstaendig konform.** `.githooks/pre-commit` zaehlt die Version
bei jedem Commit hoch; `.githooks/pre-push` vergleicht mit der
Remote-Version und bricht bei Gleichstand ab, fuehrt danach `npm run verify`
aus; `.github/workflows/ci.yml` prueft den Versionssprung gegen `HEAD~1`;
`.github/workflows/deploy.yml` fuehrt `verify` vor dem Deploy aus;
`index.html` hat das `no-cache`-Meta-Tag; `scripts/check-deploy.mjs`
vergleicht die Live-Version mit der lokalen. Kette wie in CLAUDE.md
beschrieben.

---

## 4. Doku-Konsistenz

Alle sieben in der Erkundung vermuteten Diskrepanzen wurden mit
Datei:Zeile-Beleg auf **beiden Seiten** bestaetigt. `git status` zeigt einen
sauberen Arbeitsbaum — die Werte sind aktuell, keine Zwischenstand-Artefakte.

### 4.1 Seltenheitsstufen — Punkte/XP

| Stufe | `GAME_DESIGN.md:110-117` | `rarities.ts` | Zeile |
|---|---|---|---|
| Schlicht | 1 / 1 | **2 / 2** | [37-38](src/config/rarities.ts#L37-L38) |
| Gewoehnlich | 2 / 2 | **3 / 3** | [48-49](src/config/rarities.ts#L48-L49) |
| Ungewoehnlich | 5 / 6 | **7 / 8** | [59-60](src/config/rarities.ts#L59-L60) |
| Selten | 15 / 18 | **18 / 20** | [70-71](src/config/rarities.ts#L70-L71) |
| Episch | 50 / 60 | **45 / 55** | [81-82](src/config/rarities.ts#L81-L82) |
| Legendaer | 200 / 250 | **100 / 130** | [92-93](src/config/rarities.ts#L92-L93) |

Bei den unteren fuenf Stufen liegt der Code *hoeher* als das Dokument, bei
Legendaer *niedriger* (Faktor ~2). Kein einheitlicher Umrechnungsfaktor —
die Doku ist nicht einfach eine alte Version mit globalem Faktor, sondern an
mehreren Stellen unabhaengig abweichend editiert worden.

### 4.2 Combo-Zeitfenster und -Multiplikatoren

- Dokument ([GAME_DESIGN.md:128](docs/GAME_DESIGN.md#L128), auch
  [DECISIONS.md:159](docs/DECISIONS.md#L159), ADR-0005): "2,2 s"
- Code: [`COMBO_GRACE_MS = 1800`](src/config/GameConfig.ts#L154) — 1,8 s

Beide Dokumente (GAME_DESIGN und DECISIONS) stimmen *miteinander* ueberein,
aber nicht mit dem Code — die Doku-Abweichung ist konsistent innerhalb der
Doku, nur der Code weicht ab (oder wurde seither geaendert, ohne beide
Dokumente nachzuziehen).

Multiplikatoren: Dokument nennt fuenf ganzzahlige Stufen (×1 bis ×5) bei
0/5/10/20/35. Code
([`COMBO_TIERS`](src/config/GameConfig.ts#L157-L164)) hat sechs Stufen mit
Dezimalwerten, maximal ×1,85 bei Combo 50 — strukturell und in der Hoehe
verschieden, kein Rundungsfehler.

### 4.3 XP-Formel

Dokument ([GAME_DESIGN.md:149](docs/GAME_DESIGN.md#L149)): `floor(750·√n)`.

Code ([GameConfig.ts:172-173](src/config/GameConfig.ts#L172-L173)):
```ts
Math.floor(750 * Math.sqrt(level) + 8 * Math.pow(level, 1.25))
```

Der Code hat einen zusaetzlichen Term. Kontrollrechnung Level 10: einfache
Formel ergibt ≈2372, Code-Formel ≈2514 — auch die im Dokument selbst
abgedruckte Beispieltabelle (Level 10 → 2.371 XP,
[GAME_DESIGN.md:152-159](docs/GAME_DESIGN.md#L152-L159)) passt nicht mehr zur
Code-Formel. Fuer den detaillierten Nachrechnung siehe
`docs/BALANCE_2026-08-17.md`.

### 4.4 Talente Ausdauer/Fokus/Erkenntnis/Gunst

| Talent | Dokument | Code (`talents.ts`) | Zeile |
|---|---|---|---|
| Reichweite | +6 Radius, Max 5 | +6, Max 5 | identisch |
| Flinkheit | +5% Tempo, Max 5 | +5%, Max 5 | identisch |
| Magnetismus | +35, Max 4 | +35, Max 4 | identisch |
| Ausdauer | **+4 s**, Max 4 | **+3 s** | [114](src/config/talents.ts#L114) |
| Fokus | **+250 ms**, Max 4 | **+150 ms** | [115](src/config/talents.ts#L115) |
| Erkenntnis | **+8% XP**, Max 5 | **+5%** | [116](src/config/talents.ts#L116) |
| Gunst | **+6% Punkte**, Max 5 | **+4%** | [117](src/config/talents.ts#L117) |

Auffaellig: exakt die drei Talente, die im Dokument *zuerst* stehen
(Reichweite, Flinkheit, Magnetismus), stimmen exakt — die vier danach
weichen alle ab. Deutet auf einen Bearbeitungsstand hin, bei dem der Code
weiterentwickelt, die Doku aber nur bis zur Haelfte nachgezogen wurde.

### 4.5 Welten-Belohnung

Dokument nennt einen Prozentwert je Welt, Code trennt in
`scoreMultiplier`/`xpMultiplier` ([worlds.ts:32-35](src/config/worlds.ts#L32-L35)).
Beide liegen bei jeder der 10 Welten **niedriger** als der Dokumentwert
(z.B. Horizonttor: Dokument +65%, Code +45%/+25%). Strukturell UND in der
Hoehe verschieden.

### 4.6 Erfolge-Anzahl

Dokument ([GAME_DESIGN.md:214](docs/GAME_DESIGN.md#L214)): "15 Erfolge in
vier Gruppen". Tatsaechlich: **62 Eintraege**
([achievements.ts](src/config/achievements.ts), Zeilen 41-452) in
mindestens 12 thematischen Gruppen. Das Dokument ist nicht nur ungenau,
sondern beschreibt einen fruehen Entwicklungsstand.

### 4.7 ARCHITECTURE.md — Ordnerstruktur veraltet

`docs/ARCHITECTURE.md` listet 41 Dateien, der tatsaechliche Baum hat 62. 15
real existierende Dateien fehlen im Dokument komplett, darunter ganze Scenes
(`AccountScene.ts`, `AchievementsScene.ts`, `AdminPinScene.ts`,
`AdminStatsScene.ts`, `SettingsScene.ts`, `TalentScene.ts`) und Systeme
(`AuthSystem.ts`, `ProgressSyncSystem.ts`, `SyncStatusSystem.ts`), sowie
`entities/Obstacle.ts`, `core/orientation.ts`, `core/deviceReport.ts`. Keine
im Dokument gelistete Datei fehlt umgekehrt im Code — die Doku ist reduziert
gegenueber der Realitaet, nicht falsch in dem, was sie zeigt.

### 4.8 DECISIONS.md — Stichprobe von 4 ADRs

- **ADR-0002** (keine Physik-Engine): **bestaetigt**. `main.ts` hat keinen
  `physics`-Schluessel, `GameScene.isTouching()` nutzt
  `Phaser.Math.Distance.Squared`.
- **ADR-0012** (Zugangsdaten im Repository): **bestaetigt, bewusst so**.
  `.env.production` enthaelt den Supabase-`anon`-Key im Klartext und ist
  eingecheckt (nicht gitignored). Das ist laut ADR-0012 explizit gewollt
  (publishable Key, kein `service_role`-Secret) — kein Fund, sondern eine
  Bestaetigung der dokumentierten Entscheidung.
- **ADR-0005** (Combo bricht nicht bei Fehlfang): Kernregel nicht direkt
  pruefbar aus den gelesenen Dateien, aber das Dokument selbst nennt "2,2 s"
  — derselbe veraltete Wert wie in 4.2.
- **ADR-0006** (SaveData versioniert): **bestaetigt**. `SAVE_VERSION = 6` im
  Code; der im ADR genannte Wert 2 ist explizit als historischer Nachtrag
  gekennzeichnet, kein Widerspruch, nur nicht weiter fortgeschrieben.

### 4.9 ROADMAP.md vs. Code/CHANGELOG

- **M2.6 "Login und mehrere Geraete"** ist mit 🟡 markiert, der Fliesstext
  sagt aber "sind implementiert" — intern widerspruechlich. Code
  (`AuthSystem.ts`, `ProgressSyncSystem.ts`, `SyncStatusSystem.ts`,
  `AccountScene.ts`) und `CHANGELOG.md` (Unreleased-Bereich) zeigen den
  Stand als weitgehend fertig; nur produktive SQL-Ausfuehrung und ein realer
  iOS-Geraetetest stehen laut Roadmap-Text selbst noch aus.
- **M4.1** listet unter "Naechste Meilensteine" die Forderung
  "Talentpunkte... statt Coin-Kosten" — der Code hat aber bereits das
  Gegenteil umgesetzt: ein reines Coin-System
  ([talents.ts:80-86](src/config/talents.ts#L80-L86)), mit explizitem
  Kommentar in [GameConfig.ts:175-178](src/config/GameConfig.ts#L175-L178),
  dass Talentpunkte veraltet sind. Die Roadmap-Position zeigt in die
  entgegengesetzte Richtung dessen, was der Code bereits tut.
- **M5** ist mit ✅ markiert, nennt aber im selben Punkt "Noch offen:
  Schwierigkeit und Belohnungen balancieren" — interne Inkonsistenz der
  Roadmap-Pflege, kein Code-Widerspruch.

---

## 5. Blindspot-Funde (jenseits der gruenen Testsuite)

Angewendet: die vier Techniken aus der Skill `codebase-improvement-audit`.
Jeder Fund ist ein konkretes Fehlerszenario mit Datei:Zeile, kein Verdacht.
**Kein Fix, kein Testcode wurde geschrieben** — das war ausdruecklich nicht
Teil dieses Durchgangs (nur Auflistung).

### 5.1 Am schwersten — stiller Datenverlust in `SaveSystem` — Behoben 2026-08-17

[SaveSystem.ts:176-192](src/systems/SaveSystem.ts#L176-L192) (vor dem Fix):
Waehrend einer Versions-Migration wird der migrierte Stand sofort
persistiert (Zeile 185-187, mit explizitem Begruendungskommentar: ohne
sofortiges Schreiben wuerde die Migration bei jedem Neustart wiederholt und
Waehrung vervielfacht). Der gesamte Block stand in einem `try`. Schlug
`localStorage.setItem` waehrend der Migration fehl (z.B. Kontingent
ausgeschoepft, privater Modus), griff der aeussere `catch` und ersetzte
`cache` komplett durch `createDefaultSave()` — **der migrierte,
moeglicherweise unversehrte Spielstand wurde verworfen**. Kein Replay-Pfad:
Ist `cache` einmal gesetzt, liefert `load()` bei jedem weiteren Aufruf den
Modul-Cache zurueck, ohne `localStorage` erneut zu lesen.

**Fehlerszenario:** Spieler mit vollem `localStorage`-Kontingent (z.B. durch
andere Web-Apps im selben Browser) startet die App nach einem
Versions-Update. Migration schlaegt beim Schreiben fehl → gesamter
Spielfortschritt wird durch einen leeren Stand ersetzt, ohne Fehlermeldung
an den Spieler.

**Fix:** Die Persistierung nach der Migration hat jetzt einen eigenen
`try/catch` ([SaveSystem.ts:188-196](src/systems/SaveSystem.ts#L188-L196)).
Schlaegt nur das Schreiben fehl, bleibt der migrierte Stand im
Modul-Cache gueltig — der aeussere `catch` greift nur noch bei einem Fehler
beim *Lesen* (kaputtes JSON, `getItem` wirft), nicht mehr beim Schreiben.
Belegt durch [SaveSystem.test.ts](src/systems/SaveSystem.test.ts) —
Regressionstest verifiziert per `git stash`, dass er ohne den Fix am
erwarteten Punkt fehlschlaegt (Level faellt von 12 auf 1), mit Fix gruen
ist.

### 5.2 Zentrale Verhaltensgarantie ungeprueft — `CloudSystem`

[CloudSystem.ts:6-9](src/systems/CloudSystem.ts#L6-L9): Kommentar
verspricht explizit "Jede Funktion hier [...] wirft nie [...] darf dazu
fuehren, dass man nicht mehr spielen kann." **Keine `CloudSystem.test.ts`
existiert** — diese zentrale Garantie ist durch keinen einzigen Test
abgesichert, trotz 1003 Zeilen Code mit mehrstufigem `try/catch`
([submitScoreSafely()`, Zeile 370-417](src/systems/CloudSystem.ts#L370-L417)).

### 5.3 `ChallengeSystem` — Bot- und Tages-Modus komplett ungetestet

[ChallengeSystem.test.ts](src/systems/ChallengeSystem.test.ts) deckt
ausschliesslich `kind: 'duel'` ab. `startDaily()`, `startBot()`,
`completeDaily()`, `botRound()` kommen in keinem Test vor. Konkret:

- [ChallengeSystem.ts:174-176](src/systems/ChallengeSystem.ts#L174-L176):
  Bot-Duelle schieben bei jedem `submitRound` zwei Eintraege ins Array
  (Spieler + Bot). `isComplete()` prueft bei `kind !== 'duel'` gegen `>= 1`
  ([Zeile 181](src/systems/ChallengeSystem.ts#L181)) — ein zukuenftiger
  Bugfix, der versehentlich zweimal `submitRound` im Bot-Modus ausloest,
  haette keinen Testschutz.
- [ChallengeSystem.ts:103-106](src/systems/ChallengeSystem.ts#L103-L106):
  Idempotenz-Guard gegen doppelten Tagesbonus (`lastDailyKey === key`) ist
  ungetestet — ein Fehler in der Datumsberechnung um Mitternacht/Zeitzone
  wuerde nicht auffallen.

### 5.4 `ProgressSyncSystem` — beide Guards komplett ungetestet, kein Replay

[ProgressSyncSystem.ts:39](src/systems/ProgressSyncSystem.ts#L39):
`enqueueRun()` liefert `null`, wenn nicht angemeldet oder Testprofil aktiv —
kein Event landet in der Outbox.
[ProgressSyncSystem.ts:84](src/systems/ProgressSyncSystem.ts#L84):
`flushPending()` bricht bei fehlendem `pendingDailyKey`/`pendingDailyEventId`
oder `pendingDailyCoins <= 0` ab. Schlaegt dieser Guard faelschlich fehl
(z.B. Rechenfehler in `completeDaily()` setzt `pendingDailyCoins` auf 0),
wird der Server-Claim des Tagesbonus **dauerhaft stillschweigend
uebersprungen** — kein Trigger holt ihn nach, der naechste Tag ueberschreibt
den verwaisten Key. **Keine Testdatei fuer dieses Modul.**

### 5.5 `ProgressionSystem.purchaseTalent` — Maximalrang-Zweig ungetestet

[ProgressionSystem.ts:201](src/systems/ProgressionSystem.ts#L201): Guard
`coins < cost || currentRank >= maxRank`. Der bestehende Test
([ProgressionSystem.test.ts:309-328](src/systems/ProgressionSystem.test.ts#L309-L328))
prueft nur den Coins-Mangel-Zweig. Ein Off-by-one (`>` statt `>=`) beim
Maximalrang wuerde einen Kauf ueber das Limit hinaus erlauben, ohne dass ein
Test das bemerkt.

### 5.6 `SpawnSystem.reset()` und Solo-Kapazitaetsguard ungetestet

[SpawnSystem.ts:211-213](src/systems/SpawnSystem.ts#L211-L213): `reset()`
wird von `GameScene.startRun()` genutzt, um nach dem Countdown sofort einen
Spawn auszuloesen. Kein Test ruft `reset()` auf. Der Solo-Kapazitaetsguard
([SpawnSystem.ts:117](src/systems/SpawnSystem.ts#L117),
`!this.synchronizedPositions`-Zweig) — die in jedem einzelnen Solo-Run
tatsaechlich durchlaufene Logik — wird in der Testdatei nur fuer den
Duell-Fall geprueft, nicht fuer Solo.

### 5.7 `ScoreSystem` — Multiplikator-Produkt nie im Zusammenspiel getestet

[GameScene.ts:144-148](src/scenes/GameScene.ts#L144-L148) konstruiert
`ScoreSystem` mit dem **Produkt** aus Talent- und Welt-Multiplikator. Die
Tests in `ScoreSystem.test.ts` uebergeben ausschliesslich einzelne, saubere
Skalarwerte — nie das reale Produktmuster mit den tatsaechlichen,
potenziell krummen Werten aus `talents.ts`/`worlds.ts`. Kumulierte
Rundungsfehler ueber viele Faenge sind so nie beobachtet.

### 5.8 Weitere kleinere Guard-Funde ohne Testabdeckung

- [SaveSystem.ts:143-156](src/systems/SaveSystem.ts#L143-L156):
  Migrationszweig `rawVersion < 2` (alte XP-Kurve) — einziger vorhandener
  Migrationstest deckt nur `version: 3` ab, nie `< 2`.
- [CloudSystem.ts:94](src/systems/CloudSystem.ts#L94):
  `savePendingLeaderboardScore` — Guard prueft nur bei gleicher `playerId`,
  kein Test fuer Profilwechsel-Szenario.
- [CloudSystem.ts:199-204](src/systems/CloudSystem.ts#L199-L204):
  Modul-Singleton `client ??= createClient(...)` — nie in einem Test
  verlassen, da keine `CloudSystem.test.ts` existiert.

### 5.9 Gepruefte Completeness-Behauptungen ohne Befund

Zur Ehrlichkeit: nicht jede geprüfte "alle/immer/nie"-Behauptung war falsch.
[SpawnSystem.ts:14-27](src/systems/SpawnSystem.ts#L14-L27) behauptet
gleichbleibenden RNG-Verbrauch unabhaengig vom Kapazitaetsguard — das stimmt
nachweislich fuer den Duell-Fall, korrekt begruendet und durch
`synchronizedPositions` sauber abgegrenzt. Kein Fund.

---

## 6. Performance

Aus `docs/ARCHITECTURE.md` Abschnitt 10 selbst benannte Kandidaten:

### 6.1 O(n) Kollisionstest — gemessen, kein Hotspot

Node-Benchmark des exakten Distanztest-Musters aus
[GameScene.isTouching()](src/scenes/GameScene.ts#L321-L326) gegen
realistische und hypothetische Objektzahlen:

| n (Objekte) | Zeit pro vollem Scan | Anteil am 16,6ms-Frame-Budget |
|---|---|---|
| 14 (= `MAX_ACTIVE_COLLECTIBLES`) | 0,00016 ms | 0,001 % |
| 50 | 0,00030 ms | 0,002 % |
| 1000 | 0,00284 ms | 0,017 % |

[`MAX_ACTIVE_COLLECTIBLES = 14`](src/config/GameConfig.ts) deckelt die
tatsaechlich erreichbare Objektzahl hart. Selbst bei 70-facher
hypothetischer Ueberschreitung dieses Limits bleibt der Ansatz irrelevant
fuer die Framerate. **Ergebnis: gemessen, kein relevanter Effekt.** Die in
ARCHITECTURE.md als Grenze benannte O(n)-Eigenschaft ist real, aber bei der
gegebenen Konfiguration folgenlos.

### 6.2 Kein Object Pooling — nicht gemessen, mit Begruendung

Dieser Kandidat wurde **nicht** gemessen. Begruendung: GC-Druck durch
Objekt-Allokation/-Freigabe (`new Collectible()`, `new Obstacle()` pro
Spawn) laesst sich nicht sinnvoll per Node-Benchmark isolieren — er haengt
von der tatsaechlichen Browser-Engine, Canvas-Rendering und
Geraete-GC-Verhalten ab, nicht von reiner CPU-Zeit. Eine belastbare Messung
haette echtes Geraete-Profiling erfordert (Chrome DevTools Performance-Tab
auf einem Mobilgeraet waehrend eines Runs), was ausserhalb des Umfangs
dieses Berichtsdurchgangs lag. Diese Luecke wird hier explizit benannt statt
stillschweigend übersprungen — im Sinne von Regel 8 ist "nicht gemessen"
die ehrliche Antwort, keine Vermutung ueber die Auswirkung.

---

## 7. Testabdeckung und Architektur/Duplikate

### 7.1 Ungetestete `systems/`-Dateien — Einordnung nach CLAUDE.md

CLAUDE.md fordert Tests fuer "neue Logik in `systems/`". Von den 7
ungetesteten Dateien:

| Datei | Zeilen | Nennenswerte Testbare Logik? |
|---|---|---|
| [SaveSystem.ts](src/systems/SaveSystem.ts) | 336 | **Ja** — Versions-Verzweigungen in `reconcile()`/`migrate()`, s. Abschnitt 5.1 |
| [SoundSystem.ts](src/systems/SoundSystem.ts) | 339 | **Ja** — Tier-Verzweigung in `playCollected()` (Zeile 183-247) |
| [AuthSystem.ts](src/systems/AuthSystem.ts) | 221 | **Ja** — reine Funktionen `isValidAlias()`, `isValidPin()`, `readableAuthError()` |
| [ProgressSyncSystem.ts](src/systems/ProgressSyncSystem.ts) | 118 | **Ja** — Statusuebergaenge in `flushPending()`, s. Abschnitt 5.4 |
| [SafeAreaSystem.ts](src/systems/SafeAreaSystem.ts) | 96 | Grenzfall — Verzweigung eng mit DOM/UserAgent verwoben |
| [SyncStatusSystem.ts](src/systems/SyncStatusSystem.ts) | 34 | **Nein** — reiner Getter/Setter + Enum-zu-String-Switch |

Zusatzfund ausserhalb der urspruenglich genannten Liste:
[CloudSystem.ts](src/systems/CloudSystem.ts) (1003 Zeilen, keine Testdatei)
enthaelt isolierte, reine Funktionen ohne I/O-Abhaengigkeit —
`isRemoteAhead()`/`isLocalAhead()` (Zeile 170-189), `sanitizePlayerName()`
(Zeile 507-513), `normalizeSyncCode()` (Zeile 995-1003) — die nach derselben
Regel testbar waeren, aber im selben Modul wie der Netzwerkcode liegen.

### 7.2 Duplikate zwischen Scenes

Vermutung eines groesseren Duplikationsproblems zwischen den fuenf
Account/Profil-nahen Scenes (`AccountScene`, `SyncScene`, `ProfileScene`,
`LeaderboardScene`, `SettingsScene`) **bestaetigt sich nicht in der
vermuteten Breite**. Alle fuenf nutzen konsequent dieselben Bausteine aus
`src/ui/widgets.ts` (`createBackButton`, `createPanel`,
`createMenuLayout`) — keine eigene, abweichende Implementierung gefunden.

Ein konkretes, aber kleines Duplikat besteht zwischen den zwei Dateien mit
async Lade-/Fehlerzustand:

- [AccountScene.ts:433-439](src/scenes/AccountScene.ts#L433-L439)
- [SyncScene.ts:464-470](src/scenes/SyncScene.ts#L464-L470)

Beide definieren unabhaengig voneinander ein nahezu identisches
`setStatus()`/`contentY()`-Methodenpaar (je ~6 Zeilen) auf denselben
`widgets.ts`-Hilfsfunktionen. Kandidat fuer einen gemeinsamen Helfer, aber
mit begrenztem Umfang (12 Zeilen insgesamt).

---

## 8. Pre-Flight-Check (Skill-Vorgabe)

1. Sagt der Bericht, was die Gates **nicht** sehen? — Ja, Abschnitt 2.
2. Ist jeder Fund mit Datei:Zeile und Fehlerszenario belegt? — Ja, ausser
   den explizit als Stichprobe markierten Stellen (Regel 2).
3. Tragen Performance-Aussagen ein gemessenes Vorher/Nachher? — Eine
   Messung durchgefuehrt (6.1), eine bewusst nicht (6.2, mit Begruendung).
4. Fuer jeden Fix: wer sonst erreicht den Fallback? — Der SaveSystem-Fix
   (5.1) aendert nur den Fehlerpfad des Schreibens; der Erfolgspfad und der
   Lesefehler-Pfad (kaputtes JSON) bleiben unveraendert, andere Aufrufer von
   `load()` sind nicht betroffen.
5. Fuer jeden neuen Check: welche Kanaele abgedeckt? — Der neue Test deckt
   den Schreibfehler-Kanal (`setItem` wirft) ab; der Lesefehler-Kanal
   (`getItem`/`JSON.parse` wirft) ist weiterhin ungetestet und bleibt eine
   offene Luecke (siehe 5.8, Migrationszweig `< 2`).
6. Guard-Clause-Funde gegen die zugehoerige Testdatei geprueft? — Ja, siehe
   Abschnitt 5.3-5.8, jeweils mit explizitem Ergebnis der Testsuche.
7. Ist "fixed" durch Werkzeug-Output belegt? — Ja, fuer die zwei
   "Hoch"-Funde (3.1, 5.1): `npm run verify` laeuft gruen (73/73 Tests), und
   der neue Regressionstest wurde per `git stash` gegen den vorherigen
   Code-Stand verifiziert (schlaegt ohne den Fix nachweislich fehl).

**Umfang dieses Durchgangs:** Nur die zwei mit "Hoch" eingestuften Funde
wurden auf expliziten Wunsch behoben (3.1, 5.1). Alle uebrigen Funde in
Abschnitt 5 (5.2-5.9) sind weiterhin unveraendert offen — dort wurden wie
im urspruenglichen Durchgang keine failing Tests geschrieben. Wer diese
haerter verifizieren will, findet dort pro Fund den Ansatzpunkt fuer einen
konkreten Test.
